# Codex Tasks

The shared task backlog for both Claude and Codex is:

```text
.claude/tasks/backlog.json
```

Tasks use the `LGN-` ID prefix (Liquid Glass Nav).
Status values: `backlog`, `in_progress`, `review`, `done`.

Do not create a separate backlog here.

